## Proyecto Base para una API
## Desde como utilizamos los endpoints por defecto el que viene con la plantilla de C# (WeatherForecast)
## Y la implementacion de Endpoints propios utilizando los diferentes verbos (Get, Put, Post, Delete)
## Los ejemplos utilizado utilizan el paramtro de Id, 
## la utilizacion del Explorador de los puntos de conexion (Endpoint Explorer) que funciona como si fuera el postman
## Configuracion de CORS (permiso de quien puede consumir el endpoint, normalemnte viene una ip o un dominio
## Introduccion a los middlewares "Preoceso de recibir la peticion (request) // Hacer algo incluso llamr otro md // Regreasar la peticion (response)"
## Codigo donde en un MD extraemos la ip // esto lo podriamos usar cuando se diseñe el buzon de etiquetas
##		        Console.WriteLine($"IP: {context.Connection.RemoteIpAddress}");

